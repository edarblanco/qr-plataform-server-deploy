<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <style>
    @page {
      size: 8.5in 11in;
      margin: 0.625in 0.5in;
    }
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: Arial, Helvetica, sans-serif;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(4, 1.5in);
      column-gap: 0.5in;
      row-gap: 0.5625in;
    }
    .label {
      width: 1.5in;
      height: 1.5in;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      page-break-inside: avoid;
      break-inside: avoid;
      padding: 2pt;
    }
    .qr {
      width: {{qrSize}};
      height: {{qrSize}};
      object-fit: contain;
      display: block;
      flex-shrink: 0;
    }
    .no-qr {
      width: {{qrSize}};
      height: {{qrSize}};
      display: flex;
      align-items: center;
      justify-content: center;
      background: #f0f0f0;
      font-size: 7pt;
      color: #999;
    }
    .name {
      font-size: {{nameFontSize}};
      text-align: center;
      line-height: 1.3;
      width: 1.4in;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      margin-top: 2pt;
    }
    .sku {
      font-size: 5pt;
      color: #555;
      text-align: center;
      width: 1.4in;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
  </style>
</head>
<body>
  <div class="grid">
    {{#each products}}
    <div class="label">
      {{#if ../showNameTop}}<p class="name">{{name}}</p>{{/if}}
      {{#if qrCode}}
        <img src="{{qrCode}}" alt="{{name}}" class="qr" />
      {{else}}
        <div class="no-qr">Sin QR</div>
      {{/if}}
      {{#if ../showNameBottom}}<p class="name">{{name}}</p>{{/if}}
      {{#if ../showSku}}<p class="sku">{{sku}}</p>{{/if}}
    </div>
    {{/each}}
  </div>
</body>
</html>
