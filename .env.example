# Server
PORT=4000
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://localhost:27017/qr-showroom

# JWT
JWT_SECRET=tu_super_secreto_jwt_key_aqui_cambiar_en_produccion
JWT_EXPIRATION=7d

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-correo@dominio.com
SMTP_PASS=tu-password-smtp
EMAIL_FROM=noreply@showroom.com

# Frontend URL (para QR)
FRONTEND_URL=http://localhost:5173

# Admin Email (recibe notificaciones)
ADMIN_EMAIL=admin@showroom.com

# Square API (para importar productos)
SQUARE_ACCESS_TOKEN=tu_square_access_token_aqui
SQUARE_ENVIRONMENT=sandbox
